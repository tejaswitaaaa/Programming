#include <iostream>
#include <cstdio>
using namespace std;

change(int arr[]){
    
}

int main() {
    int n,i, arr[n];
    cout<<"Enter the number of elements ";
    cin>>n;
    for(int i=0; i<n; i++){
        cin>>arr[i];
        cout<<arr[i]<<endl;
        change(arr);
    }
    return 0;
}
